export default function AdminReportsPlaceholderPage() {
  return (
    <div className="rounded-xl border border-neutral-800 bg-neutral-900 p-8">
      <h1 className="text-lg font-semibold text-white">Reports audit</h1>
      <p className="mt-2 text-sm text-neutral-400">
        Plug in moderated search + export using service role queries in a nightly
        job — never bypass RLS in the browser.
      </p>
    </div>
  );
}
